# Frontend (Next.js + TypeScript)

Aplicação Next.js consumindo a API do backend.

- Página `/` exibe status da API (`/healthz`).
- Página `/estoque` lista produtos a partir de `GET /estoque`.

Variável `NEXT_PUBLIC_API_BASE_URL` controla o endpoint do backend.